import { NotesView } from "@/features/notes/NotesView";

export function DashboardPage() {
  return (
    <NotesView
      title="All notes"
      description="Create, edit, search, pin, favorite, share, and version your notes from one workspace."
    />
  );
}
